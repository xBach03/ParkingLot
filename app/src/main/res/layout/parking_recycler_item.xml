<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/recyclerVector"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/recycler_img"
        android:src="@drawable/motorcycle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    <TextView
        android:id="@+id/recyclerText"
        android:layout_width="match_parent"
        android:layout_marginStart="40dp"
        android:layout_height="40dp"
        android:textSize="18dp"
        android:textColor="#6962AD"
        app:layout_constraintStart_toEndOf="@+id/recyclerVector"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="3dp"
        app:layout_constraintTop_toBottomOf="@+id/recyclerVector"
        app:layout_constraintStart_toStartOf="@+id/recyclerVector"
        app:layout_constraintEnd_toEndOf="@+id/recyclerText"
        android:background="@color/primaryPurple"/>

</androidx.constraintlayout.widget.ConstraintLayout>